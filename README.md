# VTMB2_SkillCalculator
A Skill Calculator created for Bloodlines 2 - an update to the spreadsheet based one I previously created

## GitHub Pages
This repository includes a GitHub Actions workflow (`.github/workflows/deploy-pages.yml`) that deploys the repository contents to GitHub Pages whenever `main` is updated (or when run manually via **workflow_dispatch**).
